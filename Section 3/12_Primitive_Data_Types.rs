fn main() {
    // Unsigned Integers
    let unsigned_num: u8 = 5; // Also: u16, u32, u64, u128, usize

    // Signed Integers
    let signed_num: i8 = -5; // Also: i16, i32, i64, i128, isize

    // Floating Point Numbers
    let float_num_32: f32 = 5.0; // 32-bit
    let float_num_64: f64 = 10.0; // 64-bit

    // Boolean
    let is_active: bool = true;

    // Character
    let letter: char = 'a';
    let emoji: char = '😊';

    // Platform-Specific Integers
    let arch_1: usize = 5;
    let arch_2: isize = -5;

    // Unit Type
    let unit: () = (); // Represents "no value"
}
